/**
 * Questo pacchetto contiene le classi dei thread che interagiscono
 * nell'algoritmo.
 * <p>
 * Vengono specificati i thread del Producer, Checker, Consumer fondamentali
 * per l'algoritmo in quanto interagiscono con il buffer, e i thread di
 * Cliente e Cuoco che servono per poter simulare l'algoritmo.
 * </p>
 */

package threads;
